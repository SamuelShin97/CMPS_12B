/*
 * Item class that has two fields one as the id and one as the priority (both are ints)
 */

public class Item
{
        int id;
        int priority;

        public Item (int i, int p)
        {
                id = i;
                priority = p;
        }
}
